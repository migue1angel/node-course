


const {SESSIONAME, USERNAME, HOME} = process.env;

// console.table({SESSIONAME, USERNAME, HOME});

const characters = ['superman', 'batman', 'wonderwoman', 'flash'];

const [ , ,wonderwoman] = characters;

// console.log(wonderwoman);